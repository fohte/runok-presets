# yaml-language-server: $schema=https://raw.githubusercontent.com/fohte/runok/main/schema/runok.schema.json

# Base preset that bundles all standard presets.
#
# Usage in your runok.yml:
#   extends:
#     - 'github:fohte/runok-presets/base'

#[test] allow: git log --oneline | head -20
#[test] allow: git shortlog -sn | head -10
#[test] allow: git ls-files | grep "\.ts$"
#[test] allow: ps aux | grep nginx
#[test] allow: cat /etc/passwd | cut -d: -f1 | sort
#[test] allow: diff <(git show HEAD:README.md) <(cat README.md)
#[test] allow: cat /etc/hosts
extends:
  - ./definitions.yml
  - ./readonly-unix.yml
  - ./readonly-git.yml

#[test] allow: bash -c "cat /etc/hosts"
#[test] ask: bash -c "rm -rf /"
#[test] allow: sudo cat /etc/shadow
#[test] allow: find . -name "*.log" -exec cat {} \;
#[test] ask: find . -name "*.log" -exec rm {} \;
#[test] allow: env FOO=bar cat /etc/hosts
#[test] allow: xargs -I{} cat {} < filelist.txt
#[test] allow: command cat /etc/hosts
#[test] ask: command rm -rf /
#[test] allow: timeout 10 cat /etc/hosts
#[test] ask: timeout 10 rm -rf /
#[test] allow: nice -n 10 cat /etc/hosts
#[test] ask: nice -n 10 rm -rf /
#[test] allow: nohup cat /etc/hosts
#[test] ask: nohup rm -rf /
#[test] allow: stdbuf -oL cat /etc/hosts
#[test] ask: stdbuf -oL rm -rf /
rules:
  # Any command with --help or --version is safe to run.
  #[test] allow: ls --help
  - allow: '* --help'
  #[test] allow: git --version
  - allow: '* --version'
